# All variables are read via pydantic-settings with the CCSWITCH_ prefix.

CCSWITCH_DATABASE_URL=sqlite+aiosqlite:///./ccswitch.db
CCSWITCH_SERVER_HOST=127.0.0.1
CCSWITCH_SERVER_PORT=41924

# Cheapest model used for the minimal /v1/messages probe.  The probe body is
# a single-character prompt with max_tokens=1 — we don't read the response
# body, only the anthropic-ratelimit-unified-* headers.
CCSWITCH_HAIKU_MODEL=claude-haiku-4-5-20251001

# Polling intervals (seconds).
CCSWITCH_POLL_INTERVAL_ACTIVE=15
CCSWITCH_POLL_INTERVAL_IDLE=300
CCSWITCH_POLL_INTERVAL_MIN=120
